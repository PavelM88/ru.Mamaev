package task1;

public class Time {
    int sec; // переменная секунды
    int min; // переменная минуты

   class TimeTest1 {
int convertacia = sec / (min * 2);
   }

    public static void main(String[] args) {
        System.out.println(3600 / 60 / 60 + " час"); // переводим секунды в часы
    }
}
